# 项目进度

## 当前阶段

v0.1.7 新增 6 个留存衰退系数，按后期留存除以前期留存计算，并接入自动显隐、独立设置页、两种排序、拖拽、列宽及填色。36 项测试通过；当前 Chrome 真实 AppLovin 报表已验证设置入口、6 列可用性、渲染和宽度，其他浏览器的本版本验收待完成，Safari 构建仍受 Xcode 工具缺失阻塞。

产品行为以 [需求文档](docs/requirements.md) 为准，工程边界以 [AGENTS.md](AGENTS.md) 为准。安装与开发入口见 [README.md](README.md)。

## 已实现

- MV3 扩展骨架，仅 storage 权限和 AppLovin 域名，无第三方运行时依赖。
- 30 个基础计算列、42 个收入增长系数、6 个留存衰退系数、ARPU 名称别名与按字段自动显隐。
- Columns 附近设置面板，搜索、独立选择、分组选择、应用／取消及本地偏好隔离。
- 收入首选与 ROAS／CPI 估算路径、零分母和异常提示、原生 Total 重算。
- 默认数值分档填色、可配置颜色区间及原生样式恢复；周期成熟度未知时不应用自定义评价色。
- 增强列拖拽及键盘调宽、宽度保存与双击恢复默认。
- 合成报表、自动化回归、静态检查和仅扩展文件的 ZIP 打包。

## 进行中与阻塞

- Safari：本机没有 Xcode.app，`xcrun --find` 无法找到 packager 或 converter。`npm run safari` 明确报错退出；需完整 Xcode 后生成工程、签名构建和 Safari 授权验收。没有安装全局依赖或更改系统设置。
- Edge／Chrome：源码共用与模拟 API 测试已完成，不代表两个浏览器均已通过本版本实际报表验收。

1. 首次导航切换消失：已修复 URL 误判并完成当前 Chrome 页面刷新后的首次展开／收起验证；完整浏览器冷启动与 Run 后立即收起仍待实测。后者尝试因工具报告用户操作 Chrome 而停止，没有反复接管。
2. 真实环境的固定列、分页、原生排序、Columns、刷新及关闭恢复，需要扩展成功加载后逐项验证。
3. AppLovin 窗口端点、整周成熟度、IAA 回传、币种及收入净额／毛额口径待核验。目前成熟度统一标记未知，自定义周期评价色暂不应用，默认颜色只表示数值大小。
4. 远期新增计算列和无周期 Unique purchasers 不属于本版范围。

## 最近完成

- 2026-09-19 16:35：v0.1.7 新增 D3/D1、D7/D3、D14/D7、D28/D7、D7/D1、D28/D1 留存衰退系数；增加独立设置页、自动展示、分组排序、默认填色和演示数据，同步双语 README 与需求文档。

- 2026-09-19 12:28：默认 `README.md` 改为英语版，新增完整中文 `README.zh-CN.md`，两份 README 顶部支持相互跳转。

- 2026-09-19 12:12：v0.1.6 不再将同账号／报表的 URL 参数变化当作请求开始；保留 Run、加载与原生表格更新的等待机制。已在当前 Chrome 重新加载源目录扩展，版本确认为 0.1.6。

- 2026-09-19 11:15：交付本地 v0.1.5，Date 固定左侧，全表指针拖动与模式独立布局保存，默认宽度改为 116／132px，保留手动宽度。

- 2026-09-18 16:01：交付本地 v0.1.4，展示类型优先分组，历史拖拽顺序仅用于组内，保留已有偏好和列宽。

- 2026-09-18 15:27：交付本地 v0.1.3 共用 ZIP，新增展示类型按钮、默认指标分类、周期分类、模式独立拖拽顺序与旧顺序迁移；新增 Safari 官方打包脚本和三浏览器使用说明。

- 2026-09-18 15:10：交付本地 v0.1.2，自动展示可计算基础指标与 ROAS 倍数、保留逐列排除、迁移旧默认设置；增强列拖拽／键盘调序和顺序保存；修复原生移除节点后的恢复。

- 2026-09-18 14:22：发布本地 v0.1.1 包，补充默认数值填色、独立开关、增强列拖拽／键盘调宽及按报表保存，保留旧设置。

- 2026-09-18 13:47：完成扩展、合成演示、19 项回归测试、静态检查入口、ZIP 打包脚本和安装文档；交付包为 `dist/applovin-cohort-enhancer-0.1.0.zip`。真实站点验收仍未完成。
- 2026-09-18 12:50：建立项目规范与需求草案，纳入独立显示开关、字段能力判断、7 组增长系数、原生布局入口及验收标准。

## 最近验证

- 2026-09-19 16:41：先新增留存系数回归并确认旧实现失败；完成后 `npm test` 36 项、`npm run check` 与 `npm run package` 通过，覆盖 6 个公式、缺字段、零分母、明细／Total 渲染和独立设置页。当前 Chrome 扩展更新到 0.1.7；真实 AppLovin 报表识别 D1／D3／D7／D14／D28 留存，设置页显示 6 项均可计算，应用后增强列从 12 增至 18，6 列均为 132px 并显示实际计算值。

- 2026-09-19 12:28：检查双语 README 的本地相互链接、命令、版本号及文档入口；`npm run check` 通过。

- 2026-09-19 12:12：新增布局 URL 回归修复前失败（增强列 0／预期 16），修复后 `npm test` 35 项及 `npm run check` 通过，含 Run 期间 URL 变化不能提前恢复。真实 Chrome 旧版刷新后出现永久等待提示；新版刷新后及首次展开／收起均显示 12 个增强列，原生默认填色保持。未重启整个浏览器，Run 后收起实测中断。

- 2026-09-19 11:15：`npm test` 33 项及 `npm run check` 通过。Chrome 合成页面滚到最右侧后 Date 表头／日期／Total 保持固定；鼠标将 Spend 拖到收入列之后，表头、明细、Total 同步。测试覆盖字段映射不变、样式恢复、视觉顺序和拖动后不误触原生点击。未代表真实 AppLovin 验收。

- 2026-09-18 16:01：在用户 Chrome 报表观察到按钮从按周期变为按指标但增强列表头顺序不变，恢复原模式。新增历史顺序回归测试先失败、修复后通过；`npm test` 31 项与 `npm run check` 全通过。新版真实页面更新后验收尚未完成。

- 2026-09-18 15:27：`npm test` 30 项通过，覆盖默认分组、周期顺序、旧顺序迁移、两套 API 命名空间、按钮切换与移除恢复、模式独立顺序。`npm run check` 通过；Safari 构建明确因工具缺失退出，未生成应用。

- 2026-09-18 15:10：复现原生移除增强节点／colgroup 后不恢复的两项测试失败，修改监听过滤后通过。`npm test` 27 项全通过，含快速加载、相同结果、自动选择、旧配置迁移、表头／明细／Total／列宽同步排序和持久化。`npm run check` 通过。真实页面尚未验收。

- 2026-09-18 14:22：`npm test` 23 项全通过；新增默认填色／关闭、自定义规则优先、旧设置迁移、宽度边界／取消／恢复、双表对齐和持久化回归。`npm run check` 与 `npm run package` 通过。新版真实站点及实际鼠标拖拽尚未验收。

- 2026-09-18 13:47：`npm test` 19 项全部通过，覆盖计算、解析、缺列恢复、行重排／复用、Total、零分母、样式恢复、Run 等待、账号隔离及取消／应用。
- 2026-09-18 13:47：Chrome 合成演示实测面板打开、增长搜索和应用；D7/D1 的 ROAS 与 RPD 均为 2.50×，缺失付费人数字段时可见列从 8 减至 4，补齐恢复 8；截图检查布局正常。这不是 AppLovin 真实页面验收。
- 2026-09-18 13:47：`npm run check` 通过语法、清单、资源引用和静态边界检查；`npm run package` 生成 ZIP，校验包内仅包含清单引用的扩展文件且 CRC 完整。
- 2026-09-18 13:47：Chrome DevTools Elements 检查真实 Arco 表格分离表头／表体、colgroup 与 tfoot，依据记录在 [页面适配说明](docs/page-adapter.md)。未读取网络响应；未确认插件运行成功。
- 2026-09-18 12:54：检查 3 份 Markdown 文档及指标样例，仅覆盖文档。
