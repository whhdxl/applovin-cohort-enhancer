# AppLovin 报表增强插件

## 项目定位与边界

为 AppLovin Ads 的 Cohort 报表增加可选计算列、收入增长系数、留存衰退／倍率系数和区间着色。当前阶段见 ROADMAP.md。

## 目录与文档职责

- `docs/requirements.md`：产品需求、指标口径、界面行为及验收标准的权威来源。
- `ROADMAP.md`：当前阶段、待办、阻塞及最近验证的权威状态记录。
- `AGENTS.md`：长期工程约定及文档导航。
- `extension/`：可直接加载的 Manifest V3 扩展。按 core、adapter、ui、content 顺序加载隔离环境中的脚本，无运行时第三方依赖。
- `tests/`：Node 测试及合成 Arco DOM，禁止复制登录信息或真实业务报表。
- `demo/`：合成报表，用同一扩展源码进行本地交互验证。
- `scripts/`：检查、打包和本地预览入口；`.tmp/<task-slug>/` 为本次任务临时内容，`dist/` 为可重建扩展包。

## 工程边界

- 原生报表、筛选、投放设置和保存行为属于 AppLovin；插件只增强已授权报表的展示。
- 指标计算独立于页面适配和渲染；收入类型、周期、同期群及分母口径必须明确。
- 不能把购买次数当作去重付费人数，不能把全体 IAA 收入当作付费用户群 IAA 收入。
- 按实际字段能力计算；缺字段、零值、空值、未成熟和估算是不同状态。
- 页面检查必须区分可访问性结构、真实 DOM 和接口响应；未检查的层次不能宣称已经确认。

## 工作流与验证入口

- 进入项目先读本文件及 `ROADMAP.md`；变更产品行为时同步更新 `docs/requirements.md`。
- 需求阶段只修改文档，不以文档交付为由启动编码、安装依赖或外部写入。
- 验证入口为 `npm test`（计算及 DOM 回归）和 `npm run check`（语法、扩展清单及安全边界）；交互与实际站点验证须单独记录证据和缺口。
